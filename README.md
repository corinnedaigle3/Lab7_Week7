#Lab7

Test to see if a managed plugin would be faster than a native plugin. Results:
 The managed plugin ended up being slower than the native plugin by 1ms, showing that C++ plugins are slightly more efficient. 
